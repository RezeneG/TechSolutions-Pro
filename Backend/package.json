# Create backend/package.json
@'
{
  "name": "techsolutions-backend",
  "version": "1.0.0",
  "description": "Backend for TechSolutions Pro",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
'@ | Out-File -FilePath "backend/package.json" -Encoding UTF8
